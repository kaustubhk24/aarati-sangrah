backend:
  name: github
  branch: main # Branch to update (optional; defaults to master)
  repo: kaustubhk24/sangrah

# These lines should *not* be indented
media_folder: "static/img/" # Media files will be stored in the repo under static/images/uploads
public_folder: "/img/" # The src attribute for uploaded media will begin with /images/uploads

collections:
- name: blog
  label: "blog"
  folder: website/blog
  identifier_field: title
  extension: md
  widget: "list"
  create: true
  slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
  fields:
    - { name: title, label: Title, widget: string }
    - { name: author, label: Label, widget: string }
    - { name: authorURL, label: "Author URL", widget: string }
    - { name: authorFBID, label: "Author FBID", widget: number }
    - { name: body, label: Body, widget: markdown }

- name: docs
  label: "docs"
  folder: docs
  identifier_field: title
  extension: md
  widget: "list"
  create: true
  fields:
    - { name: title, label: Title, widget: string }
    - { name: sidebar_label, label: Label, widget: string }
    - { name: body, label: Body, widget: markdown }

- name: aarati
  label: "Aarati"
  folder: docs/aarati
  identifier_field: slug
  extension: md
  widget: "list"
  create: true
  slug: "{{slug}}"
  fields:
    - { name: slug, slug: slug, widget: string }
    - { name: body, label: Body, widget: markdown }


- name: stotras-shlok
  label: "Stotras/Shlok"
  folder: docs/stotras-shlok
  identifier_field: slug
  extension: md
  widget: "list"
  create: true
  slug: "{{slug}}"
  fields:
    - { name: slug, slug: slug, widget: string }
    - { name: body, label: Body, widget: markdown }

- name: Ashtak
  label: "Ashtak"
  folder: docs/Ashtak
  identifier_field: slug
  extension: md
  widget: "list"
  create: true
  slug: "{{slug}}"
  fields:
    - { name: slug, slug: slug, widget: string }
    - { name: body, label: Body, widget: markdown }

- name: palana
  label: "Palana"
  folder: docs/palana
  identifier_field: slug
  extension: md
  widget: "list"
  create: true
  slug: "{{slug}}"
  fields:
    - { name: slug, slug: slug, widget: string }
    - { name: body, label: Body, widget: markdown }

- name: katha
  label: "Katha"
  folder: docs/katha
  identifier_field: slug
  extension: md
  widget: "list"
  create: true
  slug: "{{slug}}"
  fields:
    - { name: slug, slug: slug, widget: string }
    - { name: body, label: Body, widget: markdown }

- name: pooja
  label: "Pooja"
  folder: docs/pooja
  identifier_field: slug
  extension: md
  widget: "list"
  create: true
  slug: "{{slug}}"
  fields:
    - { name: slug, slug: slug, widget: string }
    - { name: body, label: Body, widget: markdown }

- name: mangalastak
  label: "Mangalashtak"
  folder: docs/managalastak
  identifier_field: slug
  extension: md
  widget: "list"
  create: true
  slug: "{{slug}}"
  fields:
    - { name: slug, slug: slug, widget: string }
    - { name: body, label: Body, widget: markdown }

- name: namavali
  label: "Namavali"
  folder: docs/namavali
  identifier_field: slug
  extension: md
  widget: "list"
  create: true
  slug: "{{slug}}"
  fields:
    - { name: slug, slug: slug, widget: string }
    - { name: body, label: Body, widget: markdown }

- name: pothi
  label: "Pothi"
  folder: docs/pothi
  identifier_field: slug
  extension: md
  widget: "list"
  create: true
  slug: "{{slug}}"
  fields:
    - { name: slug, slug: slug, widget: string }
    - { name: body, label: Body, widget: markdown }

- name: sukt
  label: "Sukt"
  folder: docs/Sukt
  identifier_field: slug
  extension: md
  widget: "list"
  create: true
  slug: "{{slug}}"
  fields:
    - { name: slug, slug: slug, widget: string }
    - { name: body, label: Body, widget: markdown }
